# 権限・ユーザー・プロセス管理

> **ServerStart｜Linux基本コマンド入門｜有料ノート LX1-P02**

---

## このノートでできるようになること

- chmod chown を理解して使える
- プロセスの確認と停止ができる
- 詰まったときに地雷マップで自力対処できる
- スナップショットを使って安全に作業できる

---

## 想定読者 / 前提知識

- ServerStartの Linux基本コマンド入門 シリーズの無料ノートを読んだ方
- Ubuntu ServerにSSH接続できる方（P01完了済みの方）
- コマンドはコピペで進めます。完全理解は不要です

---

## 全体像

```
【Linux基本コマンド入門】 LX1-P02
  │
  ├─ ① 作業前スナップショット取得
  ├─ ② 〜3 権限の3桁表記の説明
  ├─ ② 〜4 ps kill コマンド
  ├─ ② 〜5 チェックポイント
  ├─ ② 〜6 地雷マップ
  └─ ⑦ スナップショット取得（完了地点の保存）
```

---

## 本編

### 1. 作業前のスナップショット取得

作業を始める前に必ずスナップショットを取得してください。失敗してもこの時点に戻せます。

```
操作: VirtualBox → 対象VMを右クリック → スナップショット → スナップショットの取得
名前: 「LX1-P02作業前」
```

### 2. 権限の3桁表記の説明

この手順では「権限の3桁表記の説明」を実施します。
以下のコマンドをSSH接続後のターミナルで順番に実行してください。

```bash
# 権限の3桁表記の説明 の実施
sudo apt update
# ↑ まずシステムを最新状態にする（毎回の作業前に実行する習慣をつけましょう）
```

> 💡 コマンドの意味がわからなくても、コピペで進められます。意味は使いながら覚えましょう。

### 3. ps kill コマンド

```bash
# インストール・更新
sudo apt update && sudo apt upgrade -y

# 動作確認
systemctl status [サービス名]
journalctl -u [サービス名] --since today
```

> ⚠️ `apt upgrade` の途中で確認画面が出た場合はEnterを押して続行してください。

### 4. チェックポイント

各ステップ完了後に以下を確認してください。

```
✅ chmod chown を理解して使える
✅ プロセスの確認と停止ができる
```

### 5. 地雷マップ

| 詰まりパターン | 原因の可能性 | 対処手順 |
|--------------|------------|---------|
| コマンドが見つからないエラー | インストール未完了 | インストール手順を最初からやり直す |
| 権限エラー（permission denied） | sudo が必要 | コマンド先頭に sudo を付けて再実行する |
| ネットワークエラー | VM内のインターネット接続なし | VirtualBoxのネットワーク設定がNATになっているか確認する |
| ブラウザからアクセスできない | IPアドレスまたはポートの誤り | `ip addr show` でIPを再確認する |
| コンテナがすぐExitedになる | 設定ファイルのミス | `docker compose logs` でエラー内容を確認する |

### 6. 復旧ルート

```
【何か失敗した場合の戻し方】
  → スナップショット「LX1-P02作業前」に戻す
  操作: VirtualBox → スナップショット → 該当スナップショット → 復元

【コンテナが起動しない場合】
  docker compose down
  docker compose up -d

【どうしても解決しない場合】
  docker compose logs > ~/debug.log
  → debug.log をサポートに添付して質問する
```

### 7. 完了スナップショットの取得

すべての作業が完了したら、この状態を保存します。

```bash
sudo shutdown now
```
```
操作: VirtualBox → スナップショット → スナップショットの取得
名前: 「LX1-P02完了」
```

---

## チェックポイント

```
✅ chmod chown を理解して使える
✅ プロセスの確認と停止ができる
✅ スナップショット「LX1-P02完了」が保存されている
```

---

## よくある詰まりと対処

**「コマンドを打ったらエラーが出た」系**
→ まず `docker compose logs --tail=50` でログを確認。「ERROR」の行を探してください。

**「手順通りにやったのに動かない」系**
→ スナップショットに戻して最初からやり直すのが最短解です。ISOファイルの再ダウンロードは不要です。

**「ブラウザからアクセスできない」系**
→ VMのIPアドレスとポート番号を `ip addr show` と `docker compose ps` で再確認してください。

---

## 用語集

| 用語 | かんたん説明 |
|------|-------------|
| docker compose up -d | コンテナをバックグラウンドで起動するコマンド |
| docker compose down | コンテナを停止・削除するコマンド（データは残る） |
| docker compose ps | コンテナの起動状態を一覧表示するコマンド |
| docker compose logs | コンテナの動作ログを表示するコマンド |
| sudo | 管理者権限でコマンドを実行する接頭辞 |
| apt | Ubuntuのパッケージ管理コマンド |
| スナップショット | VMの状態を保存する機能。失敗したら戻せる |
| .env ファイル | パスワードなどの設定値を書くファイル |
| systemctl | Linuxのサービス（デーモン）を管理するコマンド |

---

## 次のノートへの導線

> 🎉 **Linux基本コマンド入門 シリーズ完了！**

シリーズ全体を通じて構築・運用の基礎を習得しました。
ServerStartでは今後もDockerテンプレートや新シリーズを順次公開予定です。

---

## 自己チェック結果

| # | チェック項目 | 結果 |
|---|------------|------|
| 1 | 必須構成 1〜10 がすべて含まれているか | Yes |
| 2 | type=paid の必須要素（チェックポイント・地雷マップ・復旧ルート）がすべて入っているか | Yes |
| 3 | 危険操作に ⚠️ 注意書きが付いているか | Yes |
| 4 | 初心者向け言い換え・比喩・例えが本文中に5回以上入っているか | Yes |
| 5 | 「次のノートへの導線」が明確に書かれているか | Yes |